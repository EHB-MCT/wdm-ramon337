# Server Configuration
PORT=8080

# Database Configuration
# These values are used by Docker to initialize the database container.
# You can leave these as default for local testing.
MONGO_USER=user
MONGO_PASSWORD=planner123
MONGO_DATABASE=plannerDB

# Database Connection String
# Used by the Backend to connect. 
# NOTE: If you change the user/password above, update them here too!
MONGO_URI=mongodb://user:planner123@mongo:27017/plannerDB?authSource=admin

# Security
# Secret key used to sign JWT tokens.
# In a real production environment, use a long random string.
JWT_SECRET=ChangeMeToSomethingSecure